import { Text, View } from 'react-native';
import React from 'react';

const Profile = () => {
  return (
    <View className="flex-1 flex-row justify-center items-center">
      <Text className="font-poppins-bold capitalize">Profile Page</Text>
    </View>
  );
};

export default Profile;
